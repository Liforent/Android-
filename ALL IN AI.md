豆包手机原理？

https://juejin.cn/post/7582469532326920228

**它并没有使用无障碍服务（Accessibility Service），而是使用了更底层的实现方案**：

> 豆包手机利用底层的系统权限，**直接从 GPU 缓冲区获取原始图像数据并注入输入事件，而非依赖截屏或无障碍服务**，此外手机还在一个独立的虚拟屏幕中执行后台任务，并将图像低频发送至云端进行推理，云端则返回操作指令。