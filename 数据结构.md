# 数组 Array

连续内存，相同元素，容量有限，查找快，插入慢。

# 链表 LinkedList

插入和删除快，速度O(1)，查找慢O(n). 占用内存空间多，但是可以不连续。

链表分为单链表，双向链表，循环链表，双向循环链表。

# 栈 Stack

只允许在栈顶进入入栈(push)和出栈操作(pop)。（后进先出LIFO）。

栈常用一维数组或链表来实现，用数组实现的栈叫作 **顺序栈** ，用链表实现的栈叫作 **链式栈** 。

常见应用：

1. 浏览器的回退和前进
2. 检查符号是否成对出现
3. 反转字符串
4. 维护函数调用
5. 深度优先遍历DFS

# 队列 Queue

先进先出FIFO，用数组实现的队列叫作 **顺序队列** ，用链表实现的队列叫作 **链式队列** 。**队列只允许在后端（rear）进行插入操作也就是入队 enqueue，在前端（front）进行删除操作也就是出队 dequeue**

## 队列分类

- 单队列

  存在假溢出和数组越界， `front==rear` 的时候队列为空，

- 循环队列

  可以解决假溢出和数组越界， `front==rear` 的时候队列为空或者满。

  1. 可以设置一个标志变量 `flag`,当 `front==rear` 并且 `flag=0` 的时候队列为空，当`front==rear` 并且 `flag=1` 的时候队列为满。
  2. 队列为空的时候就是 `front==rear` ，队列满的时候，我们保证数组还有一个空闲的位置，rear 就指向这个空闲位置，如下图所示，那么现在判断队列是否为满的条件就是：`(rear+1) % QueueSize==front` 。

- 双端队列Deque 

  在队列的两端都可以进行插入和删除操作的队列，相比单队列来说更加灵活。

- 优先队列 Priority Queue

  从底层结构上来讲并非线性的数据结构，它一般是由堆来实现的。

  1. 在每个元素入队时，优先队列会将新元素其插入堆中并调整堆。
  2. 在队头出队时，优先队列会返回堆顶元素并调整堆。

## 应用场景

- 阻塞队列。

  队列+阻塞操作。 队列空时，出队操作阻塞，队列满时，入队操作阻塞。使用阻塞队列可实现“生产者-消费者”模型。

- 线程池中的请求/任务队列。

- 栈。

  Stack类和Vector类在Java中已废弃，双端队列Deque可实现栈的全部功能。

- 广度优先搜索BFS。

  在图的广度优先搜索过程中，队列被用于存储待访问的节点，保证按照层次顺序遍历图的节点。



# 树 Tree

## 树的特点

1. 任意两个节点有且仅有唯一一条路径连通。
2. 一棵树如果有 n 个结点，那么它一定恰好有 n-1 条边。
3. 树不包含回路。

## 常用概念

![image-20260302182053015](/Users/mac/Library/Application Support/typora-user-images/image-20260302182053015.png)

- 高度
- 深度
- 层数

## 树的分类

### N叉树

### 二叉树

每个节点最多只有两个分支。二叉树具有左右次序不可颠倒。

#### 二叉树的分类

##### 满二叉树

每一层的节点数都是满的。节点数量 = 2^K - 1

![image-20260302182427877](/Users/mac/Library/Application Support/typora-user-images/image-20260302182427877.png)

##### 完全二叉树

除了最后一层，其余层的节点都是满的，并且最后一层是满的或者是在右边缺少连续若干节点。

![image-20260302182441739](/Users/mac/Library/Application Support/typora-user-images/image-20260302182441739.png)

##### 平衡二叉树

是一颗二叉排序树。

1. 可以是一颗空树
2. 如果不是空树，它的左右两个子树的高度差的绝对值不超过 1，并且左右两个子树都是一棵平衡二叉树。

平衡二叉树的常用实现方法有 **红黑树**、**AVL 树**、**替罪羊树**、**加权平衡树**、**伸展树** 等。

#### 二叉树的存储

- 链式存储
- 顺序存储

#### 二叉树的遍历

1. 先序遍历

   根-左-右   

2. 中序遍历

   左-根-右  

3. 后续遍历

   左-右-根

# 图 Graph

非线性结构。图就是由顶点的有穷非空集合和顶点之间的边组成的集合。通常表示为：**G(V,E)**，其中，G 表示一个图，V 表示顶点的集合，E 表示边的集合。

![image-20260302183732717](/Users/mac/Library/Application Support/typora-user-images/image-20260302183732717.png)

## 图的基本概念

顶点，边，度，权，无向图和有向图，有权图和无权图，

度：一个顶点包含多少条边，在有向图中分为出度（该顶点出去的边的条数）和入度。

权：描述边的数值

## 图的存储

### 邻接矩阵存储

![image-20260302184458913](/Users/mac/Library/Application Support/typora-user-images/image-20260302184458913.png)

值得注意的是：**无向图的邻接矩阵是一个对称矩阵，因为在无向图中，顶点 i 和顶点 j 有关系，则顶点 j 和顶点 i 必有关系。**

![image-20260302184553346](/Users/mac/Library/Application Support/typora-user-images/image-20260302184553346.png)



### 邻接表存储

针对上面邻接矩阵比较浪费内存空间的问题，诞生了图的另外一种存储方法—**邻接表** 。

邻接链表使用一个链表来存储某个顶点的所有后继相邻顶点。对于图中每个顶点 Vi，把所有邻接于 Vi 的顶点 Vj 链成一个单链表，这个单链表称为顶点 Vi 的 **邻接表**。如下图所示：

![image-20260302184651164](/Users/mac/Library/Application Support/typora-user-images/image-20260302184651164.png)

大家可以数一数邻接表中所存储的元素的个数以及图中边的条数，你会发现：

- 在无向图中，邻接表元素个数等于边的条数的两倍，如左图所示的无向图中，边的条数为 7，邻接表存储的元素个数为 14。
- 在有向图中，邻接表元素个数等于边的条数，如右图所示的有向图中，边的条数为 8，邻接表存储的元素个数为 8。

## 图的搜索

### 广度优先搜索

广度优先搜索就像水面上的波纹一样一层一层向外扩展，如下图所示：

![image-20260302184920962](/Users/mac/Library/Application Support/typora-user-images/image-20260302184920962.png)

**广度优先搜索的具体实现方式用到了之前所学过的线性数据结构——队列** 

### 深度优先搜索

深度优先搜索就是“一条路走到黑”，从源顶点开始，一直走到没有后继节点，才回溯到上一顶点，然后继续“一条路走到黑”，如下图所示：

![image-20260302184947268](/Users/mac/Library/Application Support/typora-user-images/image-20260302184947268.png)

# 堆 Heap

堆是一种满足以下条件的树：

堆中的每一个节点值都大于等于（或小于等于）子树中所有节点的值。或者说，任意一个节点的值都大于等于（或小于等于）所有子节点的值。

> 大家可以把堆(最大堆)理解为一个公司,这个公司很公平,谁能力强谁就当老大,不存在弱的人当老大,老大手底下的人一定不会比他强。这样有助于理解后续堆的操作。

**!!!特别提示：**

- 很多博客说堆是完全二叉树，其实并非如此，**堆不一定是完全二叉树**，只是为了方便存储和索引，我们通常用完全二叉树的形式来表示堆，事实上，广为人知的斐波那契堆和二项堆就不是完全二叉树,它们甚至都不是二叉树。
- （**二叉**）堆是一个数组，它可以被看成是一个 **近似的完全二叉树**。——《算法导论》第三版

![image-20260302185227115](/Users/mac/Library/Application Support/typora-user-images/image-20260302185227115.png)

# 布隆过滤器 Bloom Filter

## 什么是布隆过滤器？

布隆过滤器是一种**空间效率极高的概率性数据结构**，用于判断一个元素是否属于一个集合。它的特点是：

- **可能存在误判**（假阳性）：可能会错误地认为不在集合中的元素在集合中
- **不会漏判**（假阴性）：绝对不会错误地认为在集合中的元素不在集合中

## 实际应用场景

1. **缓存穿透保护**：防止查询不存在的数据导致数据库压力

   python

   ```
   # 查询数据前先检查布隆过滤器
   if not bloom_filter.might_contain(user_id):
       return None  # 肯定不存在，直接返回
   data = cache.get(user_id)  # 可能存在的再去查询缓存
   ```

   

2. **防止恶意请求**：过滤黑名单中的 IP 或 URL

3. **爬虫 URL 去重**：判断 URL 是否已经被爬取过

4. **数据库查询优化**：减少不必要的磁盘 I/O